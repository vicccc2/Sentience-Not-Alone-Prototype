//character class for game
/*export*/ class Conversation {
  characters // array of characters
  script // array of strings
  constructor(characters, script) {
    this.characters = characters;
    this.script = script;
  }
}
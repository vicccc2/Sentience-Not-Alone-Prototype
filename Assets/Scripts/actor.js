//Actor class for game
/*export*/  class Actor {

  // public variables
  name;     // name of character
  portrait; // image of character
  color;    // Color of their name

  // private variables (none as of now)

  constructor(name, portrait, color) {
    this.name = name;
    this.portrait = portrait;
    this.color = color;
  }
}